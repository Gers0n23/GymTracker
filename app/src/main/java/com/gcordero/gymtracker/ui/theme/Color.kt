package com.gcordero.gymtracker.ui.theme

import androidx.compose.ui.graphics.Color

// ── Brand primaries ───────────────────────────────────────────────────────────
val Primary        = Color(0xFF6366F1)   // Indigo (antes: morado #BB86FC)
val PrimaryVariant = Color(0xFF4338CA)   // Indigo oscuro
val PrimaryLight   = Color(0xFF818CF8)   // Indigo claro (gradientes, texto)
val PrimaryDim     = Color(0x266366F1)   // Indigo 15% (fondos de chips)

// ── Accent ────────────────────────────────────────────────────────────────────
val Secondary      = Color(0xFF22D3EE)   // Cyan actualizado

// ── Surfaces & backgrounds ────────────────────────────────────────────────────
val Background     = Color(0xFF0D0D0D)   // Fondo principal
val Surface        = Color(0xFF161616)   // Tarjetas nivel 1
val SurfaceElevated= Color(0xFF1E1E1E)   // Tarjetas nivel 2
val SurfaceModal   = Color(0xFF1A1A1A)   // Modales / bottom sheets

// ── Semantic ──────────────────────────────────────────────────────────────────
val Error          = Color(0xFFF87171)
val OnPrimary      = Color(0xFFFFFFFF)
val OnSecondary    = Color(0xFF000000)
val OnBackground   = Color(0xFFF0F0F0)
val OnSurface      = Color(0xFFF0F0F0)

// ── Text ──────────────────────────────────────────────────────────────────────
val TextPrimary    = Color(0xFFF0F0F0)
val TextSecondary  = Color(0xFF888888)
val TextMuted      = Color(0xFF4A4A4A)

// ── Status / muscle recovery ──────────────────────────────────────────────────
val GreenAccent    = Color(0xFF34D399)   // Recuperado / listo
val AmberAccent    = Color(0xFFFBBF24)   // Recuperándose
val RedAccent      = Color(0xFFF87171)   // Fatigado

// ── Glass effect (bordes y fondos translúcidos) ───────────────────────────────
val Glass          = Color(0x0DFFFFFF)
val GlassBorder    = Color(0x12FFFFFF)
